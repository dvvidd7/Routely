import { Feather } from "@expo/vector-icons"

export const icon = 
  {
    index:(props: any) => <Feather name='home' size={24} {...props}/>,
    two:(props: any) => <Feather name='user' size={24} {...props}/>,
  }